node_modules
**/node_modules
dist
.vite
*.local
.DS_Store

# 환경변수 — 절대 커밋 금지 (SecurityScorecard API Token 포함 가능)
.env
.env.*
!.env.example
!.env.*.example
!.env.docker.example
backend/.env

# 인증서(자체서명/실인증서) — 커밋 금지 (docker 볼륨/마운트로 관리)
docker/nginx/certs/

# 포트폴리오: 실 스크린샷이 포함된 화면정의서는 공개 저장소에서 제외(로컬 보관)
docs/SSC-화면정의서.html
docs/SSC-화면정의서.pdf

# 포트폴리오: SSC API/내부 설계 작업 문서는 공개 저장소에서 제외(로컬 보관)
docs/SSC_*.md
